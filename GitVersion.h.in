static const char VERSION[] = "Ventilation solver, "

#if PRECISE_RIEMANN
"Godunov predictor, "
#else
"Courant-Isacson-Rees predictor, "
#endif

#if ENTROPY_FIX
"with entropy fix, "
#else
"without entropy fix, "
#endif

#if FT_TRAP
"trapping floating-point exceptions, "
#endif

"revision @GIT_VERSION@, built on " __DATE__
#ifdef NDEBUG
", release version"
#else
", debug version"
#endif
;
