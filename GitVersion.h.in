static const char VERSION[] = "Ventilation solver, "

#if PRECISE_RIEMANN
"exact Riemann, "
#else
"Courant-Isacson-Rees, "
#endif

#if FT_TRAP
"trapping floating-point exceptions, "
#endif

"revision @GIT_VERSION@, built on " __DATE__
#ifdef NDEBUG
", release version"
#else
", debug version"
#endif
;
