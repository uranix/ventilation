static const char VERSION[] = "Ventilation solver, "

#if PRECISE_RIEMANN
"Godunov predictor, "
#else
"Courant-Isacson-Rees predictor, "
#endif

#if ENTROPY_FIX
"with entropy fix, "
#endif

#if TURBULENCE
"with turbulence, "
#endif

#if FP_TRAP
"with FP exceptions, "
#endif

"revision @GIT_VERSION@, built on @_date@"
#ifdef NDEBUG
", release version"
#else
", debug version"
#endif
;
